#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    

    // to take input in char string
    char str[100];
    cin.getline(str,100);
    cout<<str;
    // to take input in string
    string s;
    getline(cin,s);
    cout<<s;

      
     return 0;
}